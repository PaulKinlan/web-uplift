{
  "name": "web-uplift",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "description": "Fully agentic modern-web-quality auditing and fixing for websites: the model gathers multi-modal evidence via raw-CDP primitives and judges declarative principles. No hard-coded checks, no fast path.",
  "scripts": {
    "playground": "npx -y serve playground -l 8080",
    "evidence": "node evidence/cli.mjs",
    "audit": "node runner/run-batch.mjs",
    "batch": "node runner/run-batch.mjs",
    "aggregate": "node aggregate/aggregate.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "chrome-remote-interface": "^0.33.3",
    "serve": "^14.2.4",
    "zod": "^4.4.3"
  }
}
